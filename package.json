{
  "name": "xpmd",
  "version": "0.1.0",
  "description": "云梦Markdown desktop viewer.",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "build:icon": "node scripts/build-icon.js",
    "prepare:vendor": "node scripts/sync-vendor.js",
    "start": "npm run prepare:vendor && electron .",
    "package": "npm run prepare:vendor && npm run build:icon && node scripts/package-mac.js"
  },
  "dependencies": {
    "@highlightjs/cdn-assets": "^11.11.1",
    "dompurify": "^3.2.3",
    "electron": "^35.0.0",
    "marked": "^15.0.7"
  }
}
